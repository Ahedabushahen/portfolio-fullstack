{
  "name": "portfolio-fullstack",
  "version": "1.0.0",
  "private": true,
 "scripts": {
  "install-all": "npm install --prefix client && npm install --prefix server",
  "start": "concurrently -k -s first -n SERVER,CLIENT -c green,blue \"npm start --prefix server\" \"wait-on http://localhost:5000 && npm start --prefix client\""
}
,
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
